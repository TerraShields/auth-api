steps:
  - name: gcr.io/cloud-builders/docker
    args:
      - "build"
      - "-t"
      - "asia-southeast2-docker.pkg.dev/$PROJECT_ID/backend-dev/haptani-image"
      - "."
  - name: gcr.io/cloud-builders/docker
    args:
      - "push"
      - "asia-southeast2-docker.pkg.dev/$PROJECT_ID/backend-dev/haptani-image"
  - name: gcr.io/cloud-builders/gcloud
    entrypoint: gcloud
    args:
      - "run"
      - "deploy"
      - "api-service-backend"
      - "--image=asia-southeast2-docker.pkg.dev/$PROJECT_ID/backend-dev/haptani-image"
      - "--region=us-central1"
      - "--cpu=1"
      - "--memory=2048Mi"
      - "--allow-unauthenticated"
      - "--set-secrets=GCP_BUCKET_FOLDER=GCP_BUCKET_FOLDER:latest"
      - "--set-secrets=GCP_BUCKET_NAME=GCP_BUCKET_NAME:latest"
      - "--set-secrets=GCP_BUCKET_ID=GCP_BUCKET_ID:latest"
      - "--set-secrets=JWT_EXPIRES=JWT_EXPIRES:latest"
      - "--set-secrets=JWT_SECRET=JWT_SECRET:latest"
images:
  - "asia-southeast2-docker.pkg.dev/$PROJECT_ID/backend-dev/haptani-image"
logsBucket: "gs://capstone-bucket-bangkit-2024/logs"
